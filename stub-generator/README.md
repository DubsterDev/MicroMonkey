Stub generator generates stub-bundle.json.

To use, download the appropriate python package, such as `pip install micropython-esp32-stubs`

Then make sure that they are in `stdlib/` folder inside `stub-generator`.

Run `stub-generator.js`.